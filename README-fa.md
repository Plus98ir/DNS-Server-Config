<div align="center">

# 🌍 اسکریپت جامع راه‌اندازی سرور و مسیریابی
### [🇺🇸 English Documentation](./README.md) | [🇮🇷 توضیحات مستندات فارسی](./README-fa.md)

</div>

---

---

<div dir="rtl" style="text-align: right;">

### 📌 معرفی
این اسکریپت یک **نصاب و پیکربندی‌کننده جامع (All-in-One)** برای راه‌اندازی سرورهای مسیریابی (Routing) امن و پرسرعت است. این ابزار به صورت خودکار تمام نیازمندی‌های مدیریت ترافیک، DNS، مانیتورینگ حجم و کنترل دسترسی کاربران را نصب و تنظیم می‌کند و روی هر سرور جدیدی قابل اجرا است.

### 🚀 ویژگی‌ها و عملکردهای اسکریپت
۱. **آپدیت سیستم و پیش‌نیازها:** نصب پکیج‌های ضروری شبکه و فایروال.  
۲. **بهینه‌سازی شبکه (BBR):** فعال‌سازی الگوریتم TCP BBR جهت افزایش سرعت.  
۳. **نصب پنل X-UI ثنایی:** نصب خودکار ۳X-UI.  
۴. **پیکربندی هوشمند AdGuard Home:** تنظیم خودکار ادگارد و بستن دسترسی‌های غیرمجاز.  
۵. **مدیریت دسترسی پویا (IPSet و فایروال):** اسکریپت‌های هوشمند برای شناسایی آی‌پی کلاینت‌ها از لاگ ادگارد و هدایت ترافیک آن‌ها به تونل X-UI به صورت اتوماتیک.  
۶. **آپدیت خودکار قوانین از گیت‌هاب:** تبدیل خودکار لیست خام دامنه‌ها از گیت‌هاب به قوانین رفع تحریم (`dnsrewrite`) با آی‌پی سرور شما و آپدیت هر ۱۲ ساعت یک‌بار.  
۷. **سیستم مدیریت حجم و منوی تعاملی:** ابزارهای خط فرمان (`menu` و `hajm`) برای مشاهده مصرف دقیق پهنای باند، ریست ماهانه و مدیریت کاربران.  

---
### 🔒 نحوه احراز هویت کاربران (وایلدکارد و کلاینت‌ها)
برای اینکه سرور شما عمومی (Open Resolver) نشود و فقط افراد مجاز بتوانند متصل شوند، باید کاربران را از طریق **دامنه وایلدکارد (Wildcard)** و **شناسه کلاینت (Client ID)** در ادگارد تعریف کنید:

۱. **تنظیم دامنه Wildcard:** 
   در پنل مدیریت دامنه خود (مثلاً کلودفلر)، یک رکورد `A` با نام `*` (ستاره) بسازید و آن را به آی‌پی سرور خود متصل کنید. همچنین باید گواهینامه SSL وایلدکارد (مانند `*.yourdomain.com`) روی سرور فعال باشد.

۲. **تعریف کلاینت در AdGuard Home:**
   * وارد پنل وب ادگارد هوم شوید.
   * به مسیر **Settings** > **Client Settings** بروید و روی **Add Client** کلیک کنید.
   * نام کاربر (مثلاً `محسن`) را وارد کنید.
   * در بخش **Identifiers**، یک شناسه اختصاصی (مثلاً `mohsen`) برای او بنویسید و ذخیره کنید.

۳. **اتصال کاربر:**
   حالا کاربر می‌تواند با استفاده از این شناسه در ابتدای دامنه، به صورت کاملاً امن و رمزنگاری‌شده متصل شود:
   * **اتصال DoT (پروکسی و اندروید):** `tls://mohsen.yourdomain.com`
   * **اتصال DoH:** `https://yourdomain.com/dns-query/mohsen`

##دستورات میانبر (Aliases) پس از نصب:

با تایپ کلمه menu در ترمینال، منوی مدیریت سرور باز می‌شود.

با تایپ کلمه hajm گزارش دقیق مصرف ترافیک و مدیریت حجم نمایش داده می‌شود.

⚙️ اقدامات مهم پس از نصب (تنظیمات X-UI)
تنها کاری که باید بعد از نصب انجام دهید، تنظیم پنل X-UI ثنایی به ترتیب زیر است:

۱. دو ورودی (Inbound) در حالت Tunnel ایجاد کنید.
۲. آدرس Listen (لیسنینگ) هر دو را روی 127.0.0.1 تنظیم کنید. یکی را با پورت 443 و دیگری را با پورت 80 بسازید.
۳. حتماً در تنظیمات هر دو Inbound، گزینه Sniffing را فعال کرده و تیک‌های quic، http و tls را بزنید.
۴. در نهایت به بخش Routing Rules در پنل ثنایی بروید. این دو ورودی را به یک خروجی (Outbound) هدایت کنید (ترجیحاً در وضعیت فعلی یک کانفیگ REALITY که به سرور خارج شما متصل است، یا هر Outbound دیگری که شامل تحریم نباشد).

🔒 نحوه احراز هویت کاربران (وایلدکارد و کلاینت‌ها)
برای اینکه سرور شما عمومی (Open Resolver) نشود و فقط افراد مجاز بتوانند متصل شوند، باید کاربران را از طریق دامنه وایلدکارد (Wildcard) و شناسه کلاینت (Client ID) در ادگارد تعریف کنید:

۱. تنظیم دامنه Wildcard:
در پنل مدیریت دامنه خود (مثلاً کلودفلر)، یک رکورد A با نام * (ستاره) بسازید و آن را به آی‌پی سرور خود متصل کنید. همچنین باید گواهینامه SSL وایلدکارد (مانند *.yourdomain.com) روی سرور فعال باشد.
۲. تعریف کلاینت در AdGuard Home:

وارد پنل ادگارد هوم شوید.

به مسیر Settings > Client Settings بروید و روی Add Client کلیک کنید.

نام کاربر (مثلاً محسن) را وارد کنید.

در بخش Identifiers، یک شناسه اختصاصی (مثلاً mohsen) برای او بنویسید.

تنظیمات را ذخیره کنید.
۳. اتصال کاربر:
حالا کاربر می‌تواند با استفاده از این شناسه در ابتدای دامنه، به صورت کاملاً امن و رمزنگاری‌شده متصل شود:

اتصال DoT (پروکسی و اندروید): tls://mohsen.yourdomain.com

اتصال DoH: https://yourdomain.com/dns-query/mohsen

📜 قوانین و لیست پیش‌فرض
هنگام نصب، اسکریپت از شما لینک گیت‌هاب برای لیست دامنه‌های تحریمی را می‌خواهد. اگر هنوز لیست اختصاصی خودتان را نساخته‌اید، می‌توانید از لیست پیش‌فرض و بهینه‌شده‌ی ما استفاده کنید. کافیست لینک زیر را کپی کرده و هنگام نصب وارد کنید:
```text
(https://github.com/Plus98ir/AdGuard_Rules/blob/main/unsanction-rules.txt)
